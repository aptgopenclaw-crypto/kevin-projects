spring:
  flyway:
    baseline-on-migrate: true

jwt:
  secret: dev-only-secret-do-not-use-in-production-at-least-256-bits-long

auth:
  cookie:
    secure: false

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
